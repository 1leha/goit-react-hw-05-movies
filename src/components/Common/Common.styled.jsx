import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  margin: ${p => p.theme.space[0]}px auto;
  padding: ${p => p.theme.space[0]}px ${p => p.theme.space[3]}px;
`;
